---
import BaseLayout from "../../layouts/BaseLayout.astro";
const titoloPagina = "Contatti";

// ✅ CAMBIA QUI
const EMAIL = "ilblogdilujanta@gmail.com";
const TELEGRAM_USER = "blogdilujanta";       // senza @
const TELEGRAM_CHANNEL = "ilblogdilujanta";  // senza @
const ACADEMIA_URL = "https://independent.academia.edu/AlberoDaniela";
const X_URL = "https://x.com/ilblogdiLujanta";
const FACEBOOK_URL = "https://www.facebook.com/profile.php?id=100064996321616";
---

<BaseLayout title={titoloPagina}>
  <style is:global>
    :root{
      --ink:#2c241c;
      --green:#0d8460;
      --paper1:#efe1c5;
      --paper2:#e2caa6;
      --paper3:#d8bc93;
    }

    body{
      margin:0;
      min-height:100vh;
      background: var(--green);
      font-family:
        "Baskerville Old Face",
        "Baskerville",
        "Libre Baskerville",
        Georgia,
        "Times New Roman",
        serif;
      color:var(--ink);
    }

    main{ display:flex; justify-content:center; padding:32px 14px; }

    .paper{
      max-width:980px;
      width:100%;
      padding:60px;
      background:
        radial-gradient(120% 90% at 50% 20%, rgba(255,255,255,0.35), rgba(255,255,255,0) 55%),
        linear-gradient(180deg, var(--paper1), var(--paper2) 55%, var(--paper3));
      border-radius:6px;
      box-shadow:0 18px 60px rgba(0,0,0,0.55);
    }

    h1{
      text-align:center;
      font-size:44px;
      font-weight:400;
      margin:0 0 18px;
      letter-spacing:0.2px;
    }

.sub{
  margin: 0 0 34px;
  font-size: 20px;     /* come Testimonianze */
  line-height: 1.75;   /* come Testimonianze */
  opacity: 1;          /* niente “sbiadito” */
  text-align: left;    /* testo normale, non centrato */
}

.sub p{
  margin: 0 0 18px;    /* divisione paragrafi */
}
.sub p:last-child{
  margin-bottom: 0;
}


    .cards{
      display:grid;
      grid-template-columns: 1fr;
      gap:18px;
      margin-top: 18px;
    }
    @media (min-width: 820px){
      .cards{ grid-template-columns: 1fr 1fr; }
    }

    .card{
      border: 1px solid rgba(44,36,28,0.22);
      border-radius: 8px;
      background: rgba(255,255,255,0.20);
      padding: 18px;
    }

    /* ===== TITOLI CON ICONA ===== */
    .card-title{
      display:flex;
      align-items:center;
      gap:10px;
      margin:0 0 10px;
      font-size:22px;
      font-weight:400;
    }

    .icon{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      width:28px;
      height:28px;
      border-radius:999px;
      border:1px solid rgba(44,36,28,0.22);
      background: rgba(255,255,255,0.18);
    }

    .icon svg{
      display:block;
    }

    .card p{
      margin: 0 0 10px;
      font-size: 18px;
      line-height: 1.65;
      opacity: .92;
    }

    .contact-link{
      display:inline-block;
      text-decoration:none;
      color: var(--ink);
      border-bottom: 1px solid rgba(44,36,28,0.25);
      font-size: 18px;
      padding-bottom: 2px;
      word-break: break-word;
    }
    .contact-link:hover{ border-bottom-color: rgba(44,36,28,0.6); }

    @media (max-width:720px){
      .paper{ padding:42px 26px; }
      h1{ font-size:38px; }
      .sub{
        max-width: none; /* su mobile meglio piena larghezza */
        text-align: left;
      }
    }
  </style>

  <main>
    <article class="paper">
      <h1>Contatti</h1>

      <div class="sub">
  <p>
    Credo nello scambio consapevole e rispettoso, anche negli spazi digitali.
    Per questo motivo, nel tempo, ho scelto di non affidare il mio lavoro a un solo canale,
    ma di abitare più luoghi, selezionati per qualità e pertinenza.
  </p>

  <p>
    Internet può essere uno spazio prezioso di studio, confronto e divulgazione, se usato con misura e attenzione.
    I canali che utilizzo nascono da questa esigenza: condividere testi e materiali senza invadere, senza forzare la presenza,
    lasciando a ciascuna persona la libertà di scegliere come e quando seguire.
  </p>

  <p>
    Qui trovi i miei spazi online.
    Scegli quello che senti più vicino al tuo modo di leggere, studiare e dialogare.
  </p>
</div>


      <section class="cards" aria-label="Canali di contatto">

        <!-- EMAIL -->
        <div class="card">
          <h2 class="card-title">
            <span class="icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" width="16" height="16">
                <path d="M4 6h16v12H4z" fill="none" stroke="currentColor" stroke-width="1.8"/>
                <path d="M4 7l8 6 8-6" fill="none" stroke="currentColor" stroke-width="1.8"/>
              </svg>
            </span>
            Email
          </h2>
          <p>Per messaggi più lunghi</p>
          <a class="contact-link" href={`mailto:${EMAIL}`}>{EMAIL}</a>
        </div>

        <!-- TELEGRAM -->
        <div class="card">
          <h2 class="card-title">
            <span class="icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" width="16" height="16">
                <path d="M21 4L3.6 11.3c-.7.3-.7 1.3.1 1.5l4.5 1.4 1.7 5c.2.6 1 .7 1.4.2l2.6-3 4.8 3.5c.5.4 1.2.1 1.3-.6L22 5.2c.1-.8-.6-1.4-1-1.2z"
                  fill="none" stroke="currentColor" stroke-width="1.8"/>
                <path d="M8.2 13.9l9.7-6.2" fill="none" stroke="currentColor" stroke-width="1.8"/>
              </svg>
            </span>
            Telegram
          </h2>
          <p>Per un contatto rapido in privato</p>
          <a class="contact-link" href={`https://t.me/${TELEGRAM_USER}`} target="_blank" rel="noopener">
            @{TELEGRAM_USER}
          </a>
        </div>

        <!-- CANALE TELEGRAM -->
        <div class="card">
          <h2 class="card-title">
            <span class="icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" width="16" height="16">
                <path d="M21 4L3.6 11.3c-.7.3-.7 1.3.1 1.5l4.5 1.4 1.7 5c.2.6 1 .7 1.4.2l2.6-3 4.8 3.5c.5.4 1.2.1 1.3-.6L22 5.2c.1-.8-.6-1.4-1-1.2z"
                  fill="none" stroke="currentColor" stroke-width="1.8"/>
                <path d="M8.2 13.9l9.7-6.2" fill="none" stroke="currentColor" stroke-width="1.8"/>
              </svg>
            </span>
            Canale Telegram
          </h2>
          <p>Aggiornamenti e comunicazioni</p>
          <a class="contact-link" href={`https://t.me/${TELEGRAM_CHANNEL}`} target="_blank" rel="noopener">
            t.me/{TELEGRAM_CHANNEL}
          </a>
        </div>

        <!-- ACADEMIA -->
        <div class="card">
          <h2 class="card-title">
            <span class="icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" width="16" height="16">
                <circle cx="12" cy="12" r="9" fill="none" stroke="currentColor" stroke-width="1.8"/>
                <path d="M3 12h18" fill="none" stroke="currentColor" stroke-width="1.8"/>
                <path d="M12 3c3 3 3 15 0 18" fill="none" stroke="currentColor" stroke-width="1.8"/>
                <path d="M12 3c-3 3-3 15 0 18" fill="none" stroke="currentColor" stroke-width="1.8"/>
              </svg>
            </span>
            Academia.edu
          </h2>
          <p>I miei lavori e le mie ricerche</p>
          <a class="contact-link" href={ACADEMIA_URL} target="_blank" rel="noopener">
            Vai al profilo / articoli
          </a>
        </div>

        <!-- X -->
        <div class="card">
          <h2 class="card-title">
            <span class="icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" width="16" height="16">
                <path
                  d="M4 4l16 16M20 4L4 20"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                />
              </svg>
            </span>
            X
          </h2>
          <p>Condivisioni e aggiornamenti brevi</p>
          <a class="contact-link" href={X_URL} target="_blank" rel="noopener">
            x.com/ilblogdiLujanta
          </a>
        </div>

        <!-- FACEBOOK -->
        <div class="card">
          <h2 class="card-title">
            <span class="icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" width="16" height="16">
                <path
                  d="M14 9h3V6h-3c-2.2 0-4 1.8-4 4v2H7v3h3v5h3v-5h3l1-3h-4v-2c0-.6.4-1 1-1z"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linejoin="round"
                />
              </svg>
            </span>
            Facebook
          </h2>
          <p>Pagina e contenuti condivisi</p>
          <a class="contact-link" href={FACEBOOK_URL} target="_blank" rel="noopener">
            Vai alla pagina Facebook
          </a>
        </div>

      </section>
    </article>
  </main>
</BaseLayout>
